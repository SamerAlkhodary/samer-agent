packageMetadata:
  - repository: qlik-trial/samer-agent
    type: docker
    name: samer-agent
    apiComplianceConfig:
      specPaths:
        - {spec_path} ## TODO: Replace {spec_path} with actual path
    depConfig:
      pythonDepSpecs:
        - requirements.txt
  - repository: qlik-trial/samer-agent
    type: helm
    name: helm/samer-agent
    depConfig:
      helmDepSpecs:
        - manifests/chart/samer-agent
